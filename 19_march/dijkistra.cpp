#include<bits/stdc++.h>
using namespace std;

vector<int> dijkstra(int source,vector<vector<pair<int,int>>> &graph, vector<int> &dist)
{
    int V=graph.size();
    vector<bool> visited(V,false);
    dist[source]=0;
    for(int i=0;i<V-1;i++)
    {
        
    }
}